Fix the following issues. The issues can be from different files or can overlap on same lines in one file.

- In @backend/src/controllers/authController.ts around lines 705 - 714, The OTP generation branch only sends SMS via sendOtpSms when phone is present, so add equivalent email delivery when email is provided: after OTP creation in authController check if (email) then call an async sendOtpEmail(email, otp) (or implement one) and handle its result like sendOtpSms (await, check result.success, return res.status(500).json({...}) on failure). Also consider sending both when both phone and email are present; reference variables phone, email, otp and the existing sendOtpSms call to mirror error handling and response behavior.

- In @backend/src/controllers/authController.ts around lines 835 - 873, The verifyMsg91OTP handler currently trusts userId from the request body causing an IDOR; update verifyMsg91OTP to require an authenticated user (use the authenticated identifier from req.user or session) and stop using the client-supplied userId when calling prisma.user.update; instead resolve the target user from the authenticated principal and verify the MSG91 token is tied to that user's contact (phone/email) before updating fields (update prisma.user.update call to use the authenticated id and add a check that the token/contact match the user's stored phone/email).

- In @backend/src/middleware/validators.ts around lines 54 - 55, The password regex in validators.ts only matches a single character after the lookaheads, allowing extra/disallowed characters; update the .matches(...) pattern used in the password validation chains (the lines where .matches(/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&]).../) is used) to require one-or-more allowed characters and anchor the end (i.e., ensure the character class is followed by + and a $) so the entire string is validated, and apply the same fix to all other password validation occurrences in this file (the other .matches(...) password chains).

- In @backend/src/middleware/validators.ts around lines 284 - 298, The validators for consent fields (body('consentDataShare'), body('consentCommission'), body('declarationNotEmployed') and also consentPrivacyPolicy) mix .isBoolean() with .equals('true') which fails for boolean true; replace the .equals('true') check with a boolean-aware check such as a .custom(...) validator that asserts value === true (and returns a rejection message like "You must agree to ..."), or use .isIn([true, 'true']) if you intend to accept both boolean and string; update each field's validator (e.g., in the validator chains for consentDataShare, consentCommission, declarationNotEmployed, consentPrivacyPolicy) accordingly so they validate booleans correctly and keep the existing .withMessage text.

- In @backend/src/routes/authRoutes.ts at line 42, The route declaration router.post('/verify-msg91', otpLimiter, verifyMsg91OTP) is missing input validation middleware; add and import a validator (e.g., validateVerifyMsg91OTP) and apply it before the controller so the route becomes router.post('/verify-msg91', otpLimiter, validateVerifyMsg91OTP, verifyMsg91OTP), ensuring malformed or malicious input is rejected prior to verifyMsg91OTP execution.

- In @src/components/onboarding/StepBasicIdentity.tsx around lines 105 - 128, handleVerifyMobile currently awaits sendOTP without catching exceptions, which can leave isVerifying true if sendOTP throws; wrap the sendOTP call in a try-catch-finally inside handleVerifyMobile (use function name handleVerifyMobile and helper sendOTP to locate code), set setIsVerifying(true) before the try, call await sendOTP() inside try, on success call setOtpSent(true) and setResendTimer(30) and focus OTP input, on failure (catch) call setErrors(prev => ({ ...prev, mobileNumber: 'Failed to send OTP. Please try again.' })) and optionally log the error, and ensure setIsVerifying(false) is always called in finally so the UI never stays stuck.

- In @src/components/onboarding/StepBasicIdentity.tsx around lines 159 - 170, The handleVerifyOTP function lacks error handling and mixes state responsibilities: wrap the call to verifyOTP(otpString) in a try-catch-finally so any thrown error clears isVerifying and sets a mobileNumber error via setErrors; on successful verification call the onSuccess callback explicitly and setErrors to clear prior errors; always setIsVerifying(false) in finally to avoid leaving the spinner active; reference handleVerifyOTP, verifyOTP, setIsVerifying, setErrors, and onSuccess when making these changes.

- In @src/components/onboarding/StepBasicIdentity.tsx around lines 286 - 299, The OTP input elements rendered from otp.map lack accessible labels; update the input rendered in the map (the element that uses otpInputRefs.current, value, onChange -> handleOtpChange, onKeyDown -> handleOtpKeyDown, and onPaste -> handleOtpPaste) to include an accessible label such as aria-label={`One-time code digit ${index + 1} of ${otp.length}`} (or aria-labelledby pointing to a visually-hidden <label>), and consider adding aria-required="true" so screen readers announce purpose and position for each digit.

- In @src/components/onboarding/StepBasicIdentity.tsx around lines 172 - 182, The handleResendOTP function currently calls resendOTP without error handling or a loading guard; wrap the async call in a try-catch and add a local loading flag (e.g., isResending / setIsResending) to prevent duplicate clicks and show a spinner/disabled state while awaiting resendOTP; on success keep the existing setResendTimer(30), setOtp([...]) and clear mobileNumber error via setErrors, and on failure in the catch setErrors((prev)=>({...prev, mobileNumber: 'Failed to resend OTP. Please try again.'})) and ensure isResending is cleared in both success and error paths (also return early if resendTimer > 0 or isResending).

- In @src/components/onboarding/StepBasicIdentity.tsx around lines 35 - 51, In StepBasicIdentity.tsx inside the onSuccess and onFailure handlers, remove the console.log and console.error calls that output verification payloads (the lines inside the onSuccess: (data) => { ... } and onFailure: (error) => { ... } blocks) and replace them with either no-op logging or calls to a central sanitized logger (e.g., logger.info/logger.error) that only records non-sensitive flags (e.g., "MSG91 verification successful" or "MSG91 verification failed") and never includes the phoneVerificationToken or full error payload; ensure updateFormData, setIsVerifying, and setErrors behavior remains unchanged.

- In @src/pages/PartnerOnboarding.tsx around lines 77 - 82, The current branch only handles the successful case (response.success && response.data) and returns silently otherwise; update the submit handler that contains the response.success check so that the else path explicitly handles failures: set a visible error state or message (e.g., setSubmitError or similar), ensure any submitting/disabled flags are reset so the submit button re-enables, and show user feedback (toast or inline error) when response.success is false or response.data is missing; keep the existing localStorage.setItem, setIsSubmitted and window.scrollTo behavior in the success branch unchanged.

- In @backend/src/config/prisma.ts around lines 10 - 12, The Pool instantiation uses process.env.DATABASE_URL without validation, causing a confusing runtime error when undefined; before creating new Pool(...) validate process.env.DATABASE_URL (e.g., check truthiness) and if missing throw or log a clear error and exit so callers see "Missing DATABASE_URL" instead of a low-level pool error — update the code around the Pool creation in this module (the Pool constructor call) to perform that validation and fail fast with an explicit message.

- In @backend/src/config/prisma.ts around lines 10 - 13, The Pool and PrismaPg adapter are being instantiated at module top-level causing new connection pools on every hot-reload; move the creation of the Pool and the PrismaPg(adapter) inside the PrismaClient singleton initialization so they are created only once and tied to the same lifecycle as the cached client in globalForPrisma. Specifically, in the code that creates/returns the cached PrismaClient (the singleton block that references globalForPrisma and PrismaClient), instantiate new Pool(...) and new PrismaPg(pool) there and pass that adapter into the PrismaClient constructor; ensure any cleanup/ending of the pool is handled when the singleton is torn down.

- In @backend/src/scripts/migPostgres.ts around lines 142 - 158, The function mapAuditEventType currently defaults unknown events to 'SUSPICIOUS_ACTIVITY'; change this to a neutral fallback by adding 'UNKNOWN' to the function's return union and to the mapping Record type, include a mapping entry for 'UNKNOWN', and return 'UNKNOWN' for any unmapped event in mapAuditEventType (consider also emitting a debug/warn log when mapping falls back to 'UNKNOWN' to aid investigation).

- In @backend/src/scripts/migPostgres.ts around lines 458 - 472, The upsert block using prisma.activeSession.upsert is catching all errors and incrementing skipped, which hides real failures; change the try/catch to only treat duplicate-constraint errors as skips (inspect the thrown error in the catch of prisma.activeSession.upsert and detect Prisma unique-constraint errors via err.code === 'P2002' or Postgres SQLSTATE '23505') — if it's a duplicate, increment skipped; otherwise log or rethrow the error so migrations fail fast (do not swallow other errors), keeping DRY_RUN behavior unchanged and still incrementing migrated when DRY_RUN is true.

- In @backend/src/scripts/migPostgres.ts around lines 240 - 253, The code currently calls idMap.leads.set(mongoId, uuid) before validating the lead's partner, which leaves orphan mappings when a lead is skipped; move the idMap.leads.set call so it happens only after partnerUuid is resolved and the lead will be migrated (i.e., after the partner lookup and the partnerUuid existence check and before creating the Postgres lead record). Update the loop that iterates over leads (the for (const lead of leads) block using objectIdToUuid and idMap.leads) to only add the mapping when partnerUuid is truthy and the skipped++/continue path no longer executes.

- In @backend/src/scripts/migPostgres.ts around lines 12 - 24, Add validation for DATABASE_URL before constructing the Pool: check process.env.DATABASE_URL and if missing log a clear error and exit (similar to the existing MONGO_URI check) so Pool({ connectionString: process.env.DATABASE_URL }) is not called with undefined; update the top of the file where Pool, PrismaPg and PrismaClient are initialized (symbols: Pool, PrismaPg, PrismaClient) to perform this check and fail fast when DATABASE_URL is absent.

- In @backend/src/scripts/testDbConnection.ts around lines 8 - 9, Add an early validation that fails fast when DATABASE_URL is missing: check the dbUrl constant (from process.env.DATABASE_URL) right after it's read and if falsy log a clear error via console.error (or processLogger if used) and exit with non-zero status (process.exit(1)) so the script doesn't proceed to create the Pool; update references around the dbUrl usage in this file (testDbConnection.ts) to rely on that validation before any Pool or client creation.

- In @backend/src/services/otpChallengeService.ts around lines 34 - 45, The verifyOtpChallenge function lacks brute-force protection; modify verifyOtpChallenge to track and enforce a failure threshold by reading and updating fields on prisma.otpChallenge (e.g., failedAttempts and lockedUntil or setting otpExpiresAt to past) — before validating the OTP, check if lockedUntil > now and return {success:false, reason:'locked'}; on a failed hashOtp(otp) check, increment failedAttempts via prisma.otpChallenge.update and if failedAttempts reaches N (e.g., 3) either set lockedUntil to now + lockDuration or set otpExpiresAt = new Date(0) (invalidate) and return {success:false, reason:'locked' or 'invalid'}; on success, reset failedAttempts and clear lockedUntil (or delete the challenge) and continue to issue the token. Ensure all DB updates use prisma.otpChallenge.update and keep time comparisons using new Date().

- In @backend/src/services/otpChallengeService.ts around lines 10 - 32, The createOtpChallenge function uses crypto.randomInt(100000, 999999) which is exclusive of the upper bound and thus never yields 999999; change the call to use an exclusive upper bound one higher (e.g., crypto.randomInt(100000, 1000000)) so the full 6-digit range 100000–999999 is possible, then keep the rest of the logic (hashOtp, upsert) unchanged.

- In @backend/src/services/otpChallengeService.ts around lines 71 - 73, Replace the direct `!==` comparison of `challenge.verificationToken` and `token` with a constant-time comparison using Node's crypto.timingSafeEqual: import crypto, convert both values to Buffers (e.g. Buffer.from(challenge.verificationToken) and Buffer.from(token)), check lengths match and return false if they differ, then call crypto.timingSafeEqual on the two buffers and return that result; update the comparison in otpChallengeService.ts where `challenge.verificationToken !== token` is used.

- In @backend/src/services/smsService.ts around lines 31 - 56, The fetch call in smsService.ts lacks an HTTP status check and a request timeout; update the code that builds and sends the POST (the block creating response via fetch with body using templateId, formatIndianNumber(phone), authKey, senderId, otp) to use an AbortController with a reasonable timeout and to verify response.ok before parsing JSON — on non-ok statuses read the body safely (or include status text) and return/throw a structured error (include status and any message) so the returned object (success/message/requestId) reflects failures instead of blindly parsing error pages; ensure errors from fetch/timeout are caught and mapped to a clear failure return.

- In @src/hooks/useMsg91.ts around lines 85 - 96, The sendOTP function silently returns false when initWidget() fails due to the MSG91 script not yet loading; update the hook to expose and use a ready/loading state (e.g., isWidgetReady or widgetLoading) and change sendOTP to await the script load (with a configurable timeout) before calling widgetRef.current.sendOtp(); if the timeout elapses or initWidget still fails, return or throw a descriptive error (instead of plain false) so callers can distinguish “not ready” vs “send failed”; update references to initWidget, sendOTP, and widgetRef to check the ready flag and surface clear error messages.

- In @src/hooks/useMsg91.ts around lines 52 - 55, The cleanup in useMsg91 currently removes the shared script element in the returned unmount function (the script located via scriptId), which breaks other hook instances; fix by either removing the cleanup entirely so the script stays loaded, or implement reference counting: add a module-level or window-level counter (e.g., __msg91ScriptRefCount) that increments when useMsg91 mounts and decrements in the cleanup, and only remove the script element when the counter reaches zero; update the returned cleanup in useMsg91 to use this counter logic and ensure the script lookup still uses scriptId.

- In @src/hooks/useMsg91.ts around lines 30 - 45, The loadOtpScript retry logic creates duplicate script elements and doesn't handle final failure; update loadOtpScript (and its inner attempt function) to first check document.getElementById(scriptId) and return if an existing valid script is present, and on script.onerror remove the failed element (e.g., via element.remove()) before incrementing i and retrying; also add a terminal error path when i >= urls.length to call a provided onError callback or at least console.error so callers know all URLs failed. Ensure references: loadOtpScript, attempt, scriptId, and the onerror handler are updated accordingly.

- In @src/hooks/useMsg91.ts around lines 66 - 79, Replace the hardcoded credentials in the configuration object inside useMsg91 (the widgetId and tokenAuth fields) with values read from environment variables (e.g., process.env.NEXT_PUBLIC_MSG91_WIDGET_ID and process.env.NEXT_PUBLIC_MSG91_TOKEN_AUTH) and ensure useMsg91 falls back to an empty string if those env vars are undefined; update .env and .env.example to include NEXT_PUBLIC_MSG91_WIDGET_ID and NEXT_PUBLIC_MSG91_TOKEN_AUTH (no real secrets in example) so credentials are not committed or bundled in source.

Fix the following issues. The issues can be from different files or can overlap on same lines in one file.

- In @README.md at line 78, Update the README bullet that currently reads "SMS, Email and Kyc gateway for OTP" to use the correct acronym casing by replacing "Kyc" with "KYC" so the line reads "SMS, Email and KYC gateway for OTP".

- In @README.md at line 79, Replace the inline code-style comment "users  & roles are displayed in frontend but credentials not working" with a properly formatted Markdown list item and corrected grammar (e.g., "- Users & roles are displayed in the frontend, but credentials are not working"). Remove the leading "//", normalize spacing around "&", and ensure it follows surrounding README list styling so it renders as a bullet point.

- In @backend/.env.example around lines 28 - 29, Update the FIELD_ENCRYPTION_KEY entry to include a clear generation hint or example showing the expected format: note it must be a base64-encoded 32-byte key and add a short one-line instruction (e.g., "generate with: openssl rand -base64 32" or "Node: crypto.randomBytes(32).toString('base64')") or provide a placeholder example value; target the FIELD_ENCRYPTION_KEY line and its preceding comment so developers immediately see how to create a valid key.

- In @backend/src/controllers/authController.ts around lines 949 - 951, Replace raw mobile numbers in audit metadata with a non-reversible or masked value before calling logAuditEvent to avoid storing PII; implement and use a helper such as maskMobileNumber or hashMobile (e.g., invoked where logAuditEvent('OTP_SENT', req, { metadata: { method: 'msg91_rest_api', mobile }}) is currently used) to transform mobile into a masked or hashed string, and update all similar calls (the other occurrences around the OTP send/verify flows) to pass the transformed value instead of the raw mobile; ensure the helper uses a one-way hash or consistent masking format so logs remain useful but do not contain the plain mobile number.

- In @backend/src/controllers/authController.ts around lines 186 - 188, The code currently sets isPhoneVerified = !!phoneVerificationToken using client-supplied phoneVerificationToken, which allows bypass; change the flow to validate the token server-side instead of trusting presence: call the existing msg91VerifyOTP (or a new server-side verifier) to verify the token against the stored challenge/JWT/Redis entry and only set isPhoneVerified = true if that verification returns success; if the project uses a sentinel value like 'verified', compare phoneVerificationToken === 'verified' only after server-side validation, and reject/ignore any other truthy values. Ensure this check is performed where isPhoneVerified is computed and that unverified registrations cannot proceed.

- In @backend/src/controllers/authController.ts around lines 947 - 964, The audit log for OTP sending is recorded before verifying sendMsg91OTP's result; move the logAuditEvent('OTP_SENT', ...) call to after checking result.success inside the handler so it only runs on success (match the behavior in msg91ResendOTP), and optionally add a separate logAuditEvent('OTP_SEND_FAILED', req, { metadata: { method: 'msg91_rest_api', mobile, error: result.message } }) in the failure branch to capture failures; update references in the same function (sendMsg91OTP, logAuditEvent) accordingly.

- In @backend/src/controllers/authController.ts around lines 997 - 1001, The response currently returns a hardcoded verificationToken ('verified') which enables bypass via the verifyMsg91OTP flow; replace this with a cryptographically secure, short-lived signed token (e.g., create a JWT containing the mobile number/identifier and a short expiry) where the code currently sets data: { verificationToken: 'verified' } in the auth controller, sign it with the app secret, and return that token instead; also update the verifyMsg91OTP verification logic to validate the JWT signature and expiry (and mobile claim) rather than accepting the literal string 'verified' so verification requires a valid signed token.

- In @backend/src/controllers/authController.ts around lines 1033 - 1050, The audit currently logs 'OTP_SENT' before verifying resend success; update the flow in the resend handler so that after calling resendMsg91OTP(mobile, retryType) you check result.success first and only call logAuditEvent('OTP_SENT', req, { metadata: { method: 'msg91_rest_api_resend', mobile, retryType } }) when result.success is true; if desired, log a distinct failure event or include failure details by calling logAuditEvent with a different event name or metadata when result.success is false.

- In @backend/src/routes/adminRoutes.ts around lines 139 - 143, The response is sending the full user object (including the hashed password) via res.status(...).json(...); update the create-user response in adminRoutes.ts to strip the sensitive password field before returning: obtain a plain object from the user (e.g., via user.toObject(), user.get({ plain: true }) or similar), remove or set the password/passwordHash property to undefined, and then return that sanitized object in data (e.g., data: { user: sanitizedUser }) instead of the raw user.

- In @backend/src/routes/authRoutes.ts around lines 48 - 51, The new MSG91 OTP routes (router.post('/otp/send', msg91SendOTP), router.post('/otp/verify', msg91VerifyOTP), router.post('/otp/resend', msg91ResendOTP)) are missing input validation; add the appropriate validation middleware before the handlers—either reuse the existing validateSendOTP and validateVerifyOTP for send/verify (and use the send validator for resend), or implement MSG91-specific validators (validateMsg91SendOTP, validateMsg91VerifyOTP) if payload fields differ; ensure each route signature becomes router.post('/otp/send', otpLimiter, <validator>, msg91SendOTP), router.post('/otp/verify', otpLimiter, <validator>, msg91VerifyOTP), and router.post('/otp/resend', otpLimiter, <validator>, msg91ResendOTP).

- In @backend/src/utils/jwtValidator.ts around lines 44 - 59, The expiry-format checks use !isDevelopment while secret checks use isProduction, causing inconsistent behavior across non-production environments; update the expiry validation to use the same isProduction boolean used elsewhere (so throw only when isProduction, otherwise console.warn) for both accessExpiresIn and refreshExpiresIn, and remove the now-unused isDevelopment variable; reference validExpiryPattern, accessExpiresIn, refreshExpiresIn, and isProduction when making the change.

- In @backend/src/utils/tokenBlacklist.ts around lines 22 - 26, The constructor currently sets this.cleanupInterval = setInterval(() => this.cleanup(), 15 * 60 * 1000) but never clears it (destroy() was removed), which can leak timers in tests/hot-reload; update the constructor to call unref() on the returned interval so it won't keep the process alive (i.e., after creating the interval call this.cleanupInterval.unref() or equivalent) and restore or add a simple clear method that clears this.cleanupInterval and sets it to null to allow explicit teardown in tests; reference cleanupInterval, constructor, cleanup(), and setInterval when making the change.

- In @src/admin/pages/AdminDashboard.tsx around lines 89 - 98, The apiLeads object creation pushes loanAmount directly which can be undefined/null and later cause formatCurrency to display NaN; update the loanAmount assignment inside the apiLeads.push (the object built in AdminDashboard.tsx) to normalize missing values (e.g., coerce to a number or use a default of 0) so downstream code like formatCurrency and any comparisons always receive a valid numeric value.

- In @src/admin/types/admin.ts at line 25, LeadStatus was narrowed by removing 'docs_collected' and 'bank_logged', which is breaking; update code by either restoring those string literals to the LeadStatus union or performing a coordinated migration and code cleanup: search for all uses of LeadStatus and LeadTimelineEvent (including checks, assignments, switch/case, UI mappings, and DB model interactions), reintroduce 'docs_collected' and 'bank_logged' into the export type if the DB/backend still emits them, or otherwise add a migration to normalize existing Lead and LeadTimelineEvent records and update all logic that references those statuses to the new set; ensure the backend Lead model change is deployed and tests updated to match whichever path you choose.

- In @src/api/apiClient.ts around lines 77 - 83, The subscriber mechanism currently only stores resolution callbacks so pending requests created inside the interceptor (the Promise that calls subscribeTokenRefresh) never get rejected on refresh failure; update subscribeTokenRefresh and the refreshSubscribers storage to accept and store both resolve and reject callbacks (or a {resolve,reject} tuple), change the Promise in the interceptor that creates subscribers to pass both resolve and reject, then in the token refresh failure catch path iterate refreshSubscribers and call each stored reject(error) before clearing the array (and ensure the refresh success path calls each stored resolve(token) as before); update references to subscribeTokenRefresh, refreshSubscribers, and the Promise that resolves with apiClient(originalRequest) accordingly so no subscriber promises hang.

- In @src/components/onboarding/StepBasicIdentity.tsx around lines 182 - 185, The otpError stale-closure check causes a race; remove the conditional that checks otpError inside the resend flow and either (A) rely on the hook to set the error state (delete the if (!otpError) block and do not call setErrors here), or (B) if you must set UI errors from this component, call setErrors(...) unconditionally after the resend failure callback or use an error value returned/forwarded from the hook (i.e., propagate the hook's error into this component and use that fresh error instead of referencing otpError from a stale closure). Ensure you update the resend OTP handler where setErrors is used so it uses the current error provided by the hook rather than the otpError variable.

- In @src/components/onboarding/StepBasicIdentity.tsx around lines 164 - 171, The current post-await check reads the stale otpError closure after calling verifyOTP; instead, change the flow so you don’t rely on the otpError state after await: update verifyOTP (used in StepBasicIdentity) to return a result object like { success: boolean, error?: string } or throw an error, then after awaiting verifyOTP inspect that returned error and call setErrors((prev)=>({...prev, mobileNumber: errorMessage})) only when needed; alternatively remove the post-await otpError check entirely and rely solely on the hook's onError callback to set local errors (remove the if (!otpError) branch and any dependence on otpError in the verify flow). Ensure you update any callers of verifyOTP and references to otpError accordingly.

- In @src/stores/authStore.ts around lines 168 - 176, The partialize used when creating the auth store currently persists the entire user object (user) to localStorage, which may include PII; update the partialize in the store initialization (the object with name: 'auth-storage' and storage: createJSONStorage(...)) to persist only non-sensitive identifiers (for example user.id and user.role) and other safe flags (isAuthenticated, refreshCount), and remove email/phone/name fields from persistence; if the app needs full profile on reload, implement a rehydration flow that fetches the full user profile by id (e.g., in your auth initialization or a restoreSession function) and document the decision in comments.

- In @src/stores/authStore.ts around lines 127 - 135, The current grace-return path (when wasAuthenticated && refreshCount < 3) leaves persisted isAuthenticated/user intact causing UI to show authenticated state; update the early-return branch in the refresh logic so it clears or unpersists authentication (call set with user: null and isAuthenticated: false while incrementing refreshCount) OR stop persisting isAuthenticated and derive it from user on rehydration (adjust persistence keys where user/isAuthenticated are saved), ensuring the functions/vars set, wasAuthenticated, refreshCount, user and isAuthenticated are updated consistently so the UI cannot show authenticated state while token is invalid.

- In @src/types/shared.ts around lines 10 - 19, You removed 'docs_collected' and 'bank_logged' from the LeadStatus union in the LeadStatus type; search for all references to LeadStatus (e.g., code that compares to 'docs_collected' or 'bank_logged', switch/case in functions handling lead flow, API serializers/deserializers, and UI filters/components) and update them to use the new statuses or a mapping; add a migration or mapping layer in areas that ingest DB/API records to translate legacy values to current statuses (or explicitly handle them), update any UI display/filter logic that lists or filters by LeadStatus, and run tests to ensure no runtime errors from unhandled legacy values.

- In @backend/Dockerfile at line 28, Remove the Dockerfile command that copies tests into the image: delete the COPY src/tests ./src/tests/ line (or move it into a non-production build stage) so test files are not included in the final production image; ensure final stage only copies production sources (e.g., via existing COPY of src or a dedicated build artifact) and update any multi-stage build steps to keep tests out of the runtime image.

- In @backend/print-users.ts around lines 11 - 22, The current users query and console.table call expose PII (email, phone). Update prisma.user.findMany so the select omits email and phone unless strictly needed, or post-process the returned users array (the users variable) to mask those fields (e.g., replace characters with asterisks) before calling console.table; also gate printing behind a development-only check (process.env.NODE_ENV === 'development') or add an explicit warning flag to prevent accidental logging in CI/production. Locate the prisma.user.findMany call and the console.table(users) usage and apply one of these fixes to remove or mask PII and restrict output to local/dev environments.

- In @backend/print-users.ts around lines 26 - 29, The catch handler for main() should await Prisma disconnect and exit non‑zero on error: modify the Promise rejection handler that currently calls main().catch((e) => { console.error(e); prisma.$disconnect(); }) so it awaits prisma.$disconnect() (or calls await inside an async arrow function) and then calls process.exit(1) after logging the error to ensure cleanup completes and the process returns a failure code.

- In @backend/src/config/redis.ts around lines 52 - 56, The getRedisClient() implementation currently calls client.connect().catch(...) which only logs and returns an unconnected client; change getRedisClient() to propagate connection failures by making getRedisClient async and await client.connect(), allowing the connect error to bubble up (or explicitly re-throw the error) instead of swallowing it, so callers receive a rejected promise on failure; update any callers to await getRedisClient() or alternatively expose a client.isConnected()/health-check method, but the minimal fix is to await client.connect() inside getRedisClient() (or re-throw inside the catch) so connection errors are not hidden.

- In @backend/src/scripts/migPostgres.ts at line 189, The assignment otpHash: user.otp || null stores user.otp as-is but the field name implies a hashed value; in the migration routine in migPostgres.ts locate where otpHash is set for the user (reference symbols: otpHash and user.otp) and either (A) confirm Mongo already contains a hashed OTP and rename/or-document the source field to avoid confusion, or (B) hash the plaintext OTP before assignment using the project's standard hash utility (e.g., bcrypt/crypto helper) and store the resulting hash (preserve null when user.otp is falsy); update any related comments/variable names to reflect whether the source value is hashed.

- In @backend/src/services/otpChallengeService.ts at line 24, The OTP generation uses crypto.randomInt(min, max) which is exclusive of max, so replace the current calls that use crypto.randomInt(100000, 999999) with crypto.randomInt(100000, 1000000) to include 999999; update both the inline otp assignment (otp variable) and the dbCreateOtp call site so both generate the full 100000–999999 range.

- In @backend/src/services/otpChallengeService.ts around lines 107 - 110, dbVerifyOtp currently leaves otpHash populated which permits OTP replay; when updating on successful verification (the prisma.otpChallenge.update call that sets verifiedAt, verificationToken, verificationTokenExpires) also clear otpHash by setting otpHash: null so the OTP cannot be reused, and update the verification/expiry check logic (the OTP lookup/expiry branch around where otpHash is read) to treat a null/absent otpHash as invalid (fail verification) before comparing hashes.

- In @backend/src/services/smsService.ts around lines 86 - 91, The console.log in the sendOTP flow currently prints the user's mobile (formattedMobile) which exposes PII; update the log inside sendOTP (where MSG91 sendOTP Response is logged) to remove or mask the mobile before logging (either omit formattedMobile or replace its middle digits with asterisks, e.g. show only country code and last 2 digits) and keep the rest of the fields (status, data, templateId) unchanged so logs remain useful without leaking raw phone numbers.

- In @backend/src/services/smsService.ts around lines 38 - 47, The formatIndianNumber function currently passes through non-Indian or incorrectly sized numbers (e.g., "+123..." → "123..."); modify formatIndianNumber to validate and only return a properly formatted 12-digit Indian number (country code "91" + 10 digits) or throw/return a clear error/empty value on invalid input. Specifically, update formatIndianNumber to strip spaces/plus, enforce that the result is either exactly 12 digits starting with "91" or exactly 10 digits (then prepend "91"), and otherwise return an explicit failure (throw Error or return null/empty string). Finally, update callers of formatIndianNumber in smsService (where send/sendSms functions use it) to check for the failure value and surface a clear validation error instead of sending invalid numbers to MSG91.

- In @backend/src/services/userService.ts around lines 94 - 111, When isRedisAvailable() is true, write the OTP to both Redis and Postgres to avoid the race where Redis goes down between generateOTP and verifyUserOTP: in generateOTP (where USER_OTP_PREFIX, getRedisClient(), hashedOtp, OTP_SECONDS are used) keep the existing Redis set but also call prisma.user.update to set otpHash and otpExpires the same way as the current Redis-fallback branch; and in verifyUserOTP, if the Redis read fails or returns null, fall back to checking the stored values on the User record (otpHash/otpExpires) so verification succeeds even if Redis became unavailable.

- In @backend/src/services/userService.ts around lines 120 - 137, The verifyUserOTP function currently returns false both when OTP verification fails and when Redis is unavailable, making the result ambiguous; update verifyUserOTP to return a discriminated result (e.g. a union indicating {status: 'verified'} | {status: 'invalid'} | {status: 'use_db'}) or implement the DB fallback inside this function by checking the user's otpHash and otpExpires (use the existing user.otpHash / user.otpExpires fields) so callers can distinguish "invalid OTP" from "Redis unavailable—check DB"; adjust call sites to handle the new return shape and keep references to verifyUserOTP, USER_OTP_PREFIX, isRedisAvailable, and getRedisClient to locate the change.

- In @backend/src/tests/db.operations.test.ts around lines 189 - 200, The test uses a hardcoded email when calling prisma.user.create which breaks test isolation; replace the literal 'pii-user@example.com' with a call to the existing uniqueEmail() helper (same pattern as other tests) so the email field in the prisma.user.create data object uses uniqueEmail(), ensuring no unique-constraint collisions across test runs.

- In @backend/src/tests/redis.test.ts around lines 252 - 256, The inline comment in the 'rejects an expired OTP' test is incorrect: it claims a "0-second TTL" while the code calls redis.set(..., 'EX', 1). Update the comment to accurately state that the OTP is written with a 1-second TTL (or change the redis.set call to use a 0-second TTL if you intended immediate expiry); locate the test by its description 'rejects an expired OTP' and the redis.set call that uses `otp_challenge:${testPhone}` to apply the fix.

- In @backend/src/tests/redis.test.ts around lines 53 - 63, The cleanup currently calls flushTestKeys('rl:*') which will remove all rate-limit keys; update the pattern used in the afterAll cleanup to target only test keys (e.g., use 'rl:__test__*' or the project's test prefix) so only test-specific rate-limit entries are deleted. Modify the call to flushTestKeys in the afterAll block (alongside the other flushTestKeys calls) and keep disconnectRedis() as-is.

- In @backend/src/tests/redis.test.ts around lines 139 - 154, Tests share state causing flakiness because the same testToken (and keys with TEST_PREFIX) persist across tests; add a beforeEach (or afterEach) that clears any keys used by these tests (e.g., remove key `${TEST_PREFIX}*` or call a provided tokenBlacklist.clear/remove helper) so each spec starts with a clean Redis state; ensure you call this cleanup before the tests that use tokenBlacklist.add and tokenBlacklist.isBlacklisted so testToken is not pre-blacklisted.

- In @backend/src/utils/cache.ts around lines 87 - 98, cacheWrap currently treats a null return from fetcher() as a cache miss because cacheGet uses null for "no entry", causing repeated fetches; modify cacheWrap (and the cache storage format) to wrap cached values in a sentinel object so cached nulls are distinguishable: when setting, store an envelope like { cached: true, value: <T | null> } via cacheSet; when getting, inspect the envelope returned by cacheGet and return envelope.value (including null) if envelope.cached is true, otherwise treat as miss and call fetcher(); keep function names cacheWrap, cacheGet, cacheSet and DEFAULT_TTL_SECONDS to locate changes.

- In @backend/src/utils/fieldEncryption.ts around lines 208 - 222, The set actionsWithResults incorrectly includes 'deleteMany' and 'updateMany' which return only { count: number } rather than record(s); update the Set in fieldEncryption.ts (the variable actionsWithResults) to remove 'deleteMany' and 'updateMany' so decryptResult(m, result) is not called for those operations, and ensure any other code that relies on actionsWithResults for decrypting results (decryptResult invocation) remains unchanged.

- In @backend/src/utils/fieldEncryption.ts around lines 46 - 67, decryptString currently calls decipher.final() directly which can throw on corrupted ciphertext or auth-tag mismatch; wrap the decryption steps (creation of decipher, setAuthTag, update, final) in a try-catch inside decryptString and handle failures by logging the error and returning null (or propagating a controlled error) instead of allowing an uncaught exception; reference decryptString, crypto.createDecipheriv, decipher.setAuthTag and decipher.final in your changes and ensure getEncryptionKey/isEncrypted behavior remains unchanged.

- In @backend/src/utils/fieldEncryption.ts around lines 96 - 148, encryptWhere currently encrypts equals/in/notIn/not and skips string filters like contains/startsWith/endsWith on encrypted fields (ENCRYPTED_FIELDS), which would silently produce incorrect queries; update encryptWhere to detect unsupported string filter keys (e.g., contains, startsWith, endsWith and any other non-exact match ops) when processing a field listed in ENCRYPTED_FIELDS and throw a clear error (including the model and field name) instead of allowing the query to run, ensuring this check runs both in the top-level branch that handles value objects (the filter variable) and inside the boolean operator branch (AND/OR/NOT) so nested filters are validated; continue using encryptString for allowed ops (equals, in, notIn, not) and keep recursion for nested objects.

- In @backend/src/utils/fieldEncryption.ts around lines 23 - 25, deriveIv currently derives a deterministic 12-byte IV from the plaintext (function deriveIv) which breaks AES-GCM security by producing repeat nonces; replace this deterministic IV strategy with a secure per-encryption random nonce (e.g. crypto.randomBytes(12)) in the encryption path (where deriveIv is used) and include/prefix the random IV with the ciphertext so decryption reads it back, or—if deterministic encryption is truly required for queryability—replace the AES-GCM usage with a deterministic AEAD designed for that purpose (e.g., AES-SIV / AES-GCM-SIV) and update the corresponding encrypt/decrypt functions to use that library/API instead of deriveIv.

- In @backend/src/utils/msg91.ts around lines 15 - 18, The function verifyMsg91Token was changed to always return false which silently breaks callers; search the repo for any usages of verifyMsg91Token and either (A) remove/replace those calls with the new smsService.verifyOTP flow, or (B) if callers still exist before migration, change verifyMsg91Token to throw a clear Error("verifyMsg91Token is deprecated; use smsService.verifyOTP") instead of returning false so failures are explicit; update any affected tests and exports accordingly and run the test suite to ensure all call sites are migrated or updated.

- In @nginx.conf around lines 24 - 34, The Location block for /api/ sets Connection 'upgrade' for all requests which breaks HTTP keep-alive; add a map directive (e.g. map $http_upgrade $connection_upgrade { default upgrade; '' close; }) in the http context or an included file and then change the proxy_set_header in the location /api/ block to use the mapped variable (proxy_set_header Connection $connection_upgrade) so Connection is only 'upgrade' when the client requests an upgrade.

- In @src/admin/components/leads/AddLeadModal.tsx around lines 76 - 80, The current validation in AddLeadModal uses Number(formData.loanAmount) <= 0 which lets non-numeric strings pass because Number("abc") is NaN; update the validation for formData.loanAmount to first parse the value and check for NaN (e.g., Number or parseFloat) and finite number before comparing to 0, and set nextErrors.loanAmount to an appropriate message when the value is not a valid number or when it is <= 0 so that non-numeric input is rejected.

- In @src/components/application-form/SuccessPopup.tsx around lines 43 - 45, The requestAnimationFrame callback currently always tries to focus closeButtonRef.current which is null when onClose is undefined; change the focus logic in the requestAnimationFrame callback to fall back to a programmatically focusable element (e.g., modalContainerRef or headingRef) when closeButtonRef.current is falsy, and ensure the modal container element has tabindex="-1" so it can receive focus; update the component to create and use modalContainerRef (or headingRef) and call modalContainerRef.current?.focus() as the fallback inside the same RAF callback that currently references closeButtonRef.

- In @src/hooks/useLocalStorage.ts around lines 79 - 86, The storage event handler currently skips JSON.parse when initialValueRef.current is a string, causing double-encoded strings; change the handler in useLocalStorage to always JSON.parse(event.newValue) (inside a try/catch) and then call setStoredValue(parsed as T), falling back to initialValueRef.current and logging a warning on parse errors so behavior matches readValue; reference initialValueRef, setStoredValue, readValue, key and the storage event handling logic when making the change.

- In @src/hooks/useLocalStorage.ts at line 33, The dispatched StorageEvent in useLocalStorage.ts is missing newValue, causing listeners to treat the change as a removal; update the dispatch inside the setValue logic (where window.dispatchEvent is called) to include a serialized newValue (e.g., JSON.stringify(value)) in the event init object (and optionally include oldValue/storageArea if desired) so the storage listener sees the correct non-null newValue and doesn't reset storedValue to initialValue.

- In @src/hooks/useMsg91.ts around lines 66 - 69, The configuration object in useMsg91.ts currently hardcodes credentials (widgetId and tokenAuth); replace those literal values with environment variables (e.g., process.env.MSG91_WIDGET_ID and process.env.MSG91_TOKEN_AUTH) in the configuration assignment inside the useMsg91 hook, add a safe fallback or throw a clear error if they are missing, and ensure any tests or callers of configuration/identifier still pass identifier || ''. Also update deployment/.env and rotate the exposed credentials out of source control.

- In @src/hooks/useMsg91.ts around lines 100 - 104, The catch in sendOtp inside useMsg91 currently returns true on exception which hides real failures; change the catch to return a definitive failure or an explicit result object (e.g., { success: boolean, uncertain?: boolean }) instead of true—capture and log the error with processable details and return either false (or { success: false, uncertain: true } if you want to express ambiguity); update sendOtp's return type and adjust all callers to handle the new boolean or discriminated-union result so failures can be handled properly.

- In @src/hooks/useMsg91.ts around lines 30 - 45, The loadOtpScript retry logic leaves failed <script> elements in the DOM which creates duplicates for the same scriptId; update the attempt/onerror flow in loadOtpScript so that when s.onerror fires it removes the failed element (the script element with id scriptId or the local s) from document.head before incrementing i and calling attempt() again, and also ensure attempt checks for and avoids appending a new element if an existing script with scriptId is already present; reference the loadOtpScript function, the attempt inner function, the s.onerror handler, and the scriptId identifier when making the change.

- In @src/hooks/usePagination.ts around lines 84 - 91, The clamp uses pageSize from a possibly stale closure in usePagination: update setTotalItems to accept an optional newPageSize parameter (e.g., setTotalItems(total: number, newPageSize?: number)) and compute newTotalPages from newPageSize ?? current pageSize state before calling setCurrentPage, or alternatively refactor page and pageSize into one state (useReducer or single object) so updates to pageSize and totalItems are applied atomically; adjust call sites (including any setPageSize usages that call setTotalItems in the same handler) to pass the new pageSize when available.

- In @src/hooks/usePagination.ts around lines 78 - 82, The setPageSize callback can accept NaN or non-finite values because it uses Math.floor/coercion without checking finiteness; update setPageSize to mirror validatedInitialPageSize by first checking Number.isFinite(size) (or coerce to a finite default) before flooring and clamping, then call setPageSizeState(coerced) and setCurrentPage(1); ensure the function guards against NaN/Infinity so Math.max(1, ...) never receives NaN and pagination state cannot be corrupted.

- In @src/tests/leadsApi.test.ts at line 99, The test currently uses an unsafe assertion `as never` when calling updateLead('lead-1', { loanAmount: 6000000 } as never, true); which disables type checking; replace that cast with a proper partial payload type such as Partial<LeadData> (or the specific update payload type exported by your API module) and update the call to pass the object as that type, or update the test signature/imports to use the correct UpdateLeadPayload type so TypeScript can validate the shape rather than suppressing errors.